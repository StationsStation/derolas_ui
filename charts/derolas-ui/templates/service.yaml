apiVersion: v1
kind: Service
metadata:
  name: {{ include "derolas-ui.fullname" . }}
  labels:
    {{- include "derolas-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "derolas-ui.selectorLabels" . | nindent 4 }}
